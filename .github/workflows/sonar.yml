name: SonarQube & Trivy Analysis

on:
  push:
    branches:
      - main

jobs:
  security-and-quality:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          format: 'table'
          severity: 'CRITICAL,HIGH'

      - name: SonarQube Scan
        uses: sonarsource/sonarqube-scan-action@v3 # Changement de version master -> v3
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: https://70f17292c49c9fb6-154-124-201-171.serveousercontent.com
        with:
          args: >
            -Dsonar.projectKey=mon-projet-test